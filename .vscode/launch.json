{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current Crate",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/${input:crateName}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "sourceLanguages": ["rust"]
        }
    ],
    "inputs": [
        {
            "id": "crateName",
            "type": "promptString",
            "description": "Enter crate name (e.g., problem1)"
        }
    ]
}

